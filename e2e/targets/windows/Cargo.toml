[package]
name = "windows"
version = "0.0.0"
edition = "2021"
publish = false

[dev-dependencies]
hidra = { path = "../../.." }
conformance = { path = "../../shared/conformance" }

[target.'cfg(target_os = "windows")'.dev-dependencies]
libloading = "0.8"
